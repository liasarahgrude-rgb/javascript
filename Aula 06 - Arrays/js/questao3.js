// Você tem um array carrinho(de compras) com dois itens. Adicione um novo item no final 
// usando push(). Depois, remova o item que está no início usando shift().
let carrinho["Celular" ; "Tablet"]
carrinho.push ("Notebook")
carrinho.shift()
console.log(carrinho)
