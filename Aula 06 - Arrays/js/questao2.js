// Crie um array chamado numeros com 5 valores. Use a propriedade .length e um loop for 
// para imprimir no console cada número, um por linha.
let numeros = [10 , 20 , 30, 40, 50]
numeros.lenght
for(let i = 0; i < numeros.lenght;i++){
      console.log("Numero ", numeros[i])
}

