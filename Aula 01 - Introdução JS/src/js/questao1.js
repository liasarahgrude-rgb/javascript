// <!-- Crie um arquivo HTML (atividade1.html). 
// Dentro da tag <script>, escreva um código JavaScript que: 
// Declare uma variável chamada nomeAluno usando let e atribua a ela o seu próprio nome (em String). 
// Declare uma constante chamada ANO_ATUAL usando const e atribua o ano de 2025 (em Number). 
// Use console.log() para exibir a seguinte frase no Console do Navegador (use o operador + para unir as variáveis e a string): "O aluno [Nome do Aluno] está estudando JS no ano de [Ano Atual]." -->
let nomeAluno = "Lia Sarah";
const ANO_ATUAL = 2025;
console.log("O aluno", nomeAluno, "está estudando JS no ano de", ANO_ATUAL)